// Tiernan Lindauer 134003853

/**
 * And gate: 
 * out = 1 if (a == 1 and b == 1)
 *       0 otherwise
 */

CHIP And {
    IN a, b;
    OUT out;

    PARTS:
    Nor(a=a, b=b, out=notAnd);
    Nor(a=notAnd, b=notAnd, out=out);
}
